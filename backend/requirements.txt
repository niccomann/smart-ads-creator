# NOTE: First run: source ../install-shared-libs.sh (or ../../install-shared-libs.sh)
# This installs shared-fastapi-bootstrap and shared-llm-gateway as editable packages.

# FastAPI & Server
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
python-multipart>=0.0.12
websockets>=14.0

# Pydantic & Validation
pydantic>=2.10.0
pydantic-settings>=2.6.0

# HTTP & Async
httpx>=0.28.0
aiofiles>=24.1.0

# Database (SQLite for simplicity - single user)
sqlalchemy>=2.0.36
aiosqlite>=0.20.0

# Utils
python-dotenv>=1.0.1
loguru>=0.7.2

# Apify for scraping
apify-client>=1.8.1

# OpenAI for Sora video generation
openai>=1.57.0

# Runway ML for video generation
runwayml>=0.1.0

# Google GenAI for Veo video generation
google-genai>=1.0.0
